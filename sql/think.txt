---session_type

/* CREATE TABLE IF NOT EXISTS session_type (

    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    description TEXT,
    duration_hours INT NOT NULL,
    price DECIMAL (10,2) NOT NULL,
    is_active BOOLEAN NOT NULL DEAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP

) ENGINE=InnoDB;

---bookings
CREATE TABLE IF NOT EXISTS bookings (

    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    session_type_id INT NOT NULL,
    scheduled_at DATETIME NOT NULL,
    price_paid DECIMAL(10,2) NOT NULL,
    status ENAM,('pending','paid','cancelled', 'completed')NOT NULL DEFAULT pending,
    payment_ref VARCHAR(100) DEFAULT NULL,
    notes TEXT,
    crated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP

    FOREIGN KEY (user_id)REFERENCES users(id) ON DELETE CASCADE,
    FOREIGN KEY (session_type_id)REFERENCES session_type(id)

) ENGINE=InnoDB;

---products
CREATE TABLE IF NOT EXISTS products (

    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    description TEXT,
    duration_hours INT NOT NULL,
    price DECIMAL (10,2) NOT NULL,
    is_active BOOLEAN NOT NULL DEAULT TRUE,
    file_path VARCHAR(255) DEFAULT NULL,
    image_path VARCHAR(255) DEFAULT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP

) ENGINE=InnoDB;

---purchases
CREATE TABLE IF NOT EXISTS purchases (

    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    product_id INT NOT NULL,
    price_paid DECIMAL(10,2) NOT NULL,
    status ENUM,('pending','paid','cancelled','refunded','completed')NOT NULL DEFAULT pending,
    payment_ref VARCHAR(100) DEFAULT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP

    FOREIGN KEY (user_id)REFERENCES users(id) ON DELETE CASCADE,
    FOREIGN KEY (product_id)REFERENCES products(id)

) ENGINE=InnoDB;*/

---products